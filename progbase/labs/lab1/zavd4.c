#include<stdio.h>
#include<math.h>
int main(void){
int n , m ;
double sum , x, i, j;
	printf("write integer n=");
	scanf("%i" , &n);
	printf("write integer m=");
	scanf("%i" , &m);
	if (n < 1 && j < 1)
		printf("некоретно введено");
			sum = 0;
				for(j = 1; j <= m; j++)
					sum += (3 + 1 / (j + 3));
			for(i = 1; i <= n; i++){
				x += i * sum;
}
printf("%.10f\n", x);
return 0;
}
